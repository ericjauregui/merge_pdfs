[project]
name = "merge-pdfs"
version = "0.1.0"
description = "Simple utility to merge PDFs from input/ into output/."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pypdf2>=3.0.1",
]
